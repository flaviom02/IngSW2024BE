spring:
  application:
    name: EventManager
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: ingsw
    url: jdbc:mysql://localhost:3306/events_db
    username: ingsw
  jpa:
    hibernate:
      ddl-auto:
        update
      dialect:
        org.hibernate.dialect.MySQL8Dialect
  properties:
    sericeUrls:
      categories: http://localhost:3001/api/
    scheduleInterval:
      categories: 300000